exports.queryList={
    USERS_SIGNUP_QUERY : 'INSERT INTO BOOKING.USERS (USER_FIRSTNAME, USER_SURNAME, USER_EMAIL, USER_PASSWORD, USER_PHONE) VALUES($1,$2,$3,$4,$5);',
    AUTHORIZATION :'SELECT * FROM BOOKING.USERS WHERE USER_EMAIL =$1 AND  USER_PASSWORD =$2',
    CART_ITEMS:`SELECT B.BOOKED_ID , B.STATE , C.CLUB_NAME ,C.URL_IMAGE, B.BOOKED_DATA_WORK ,
    B.BOOKED_TIME_START , B.BOOKED_TIME_END , C.CLUB_PRICE 
    FROM BOOKING.BOOKED B , BOOKING.CLUB C
    WHERE B.CLUB_ID = C.CLUB_ID AND B.USER_ID=$1`,
    CLUB_CART_ITEME:'SELECT  CLUB_NAME, CLUB_LOCATION, CLUB_PRICE, URL_IMAGE, CLUB_DESCRIPTION FROM BOOKING.CLUB WHERE club_id=$1',
    
    

}